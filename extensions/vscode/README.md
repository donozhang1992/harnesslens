# HarnessLens VS Code Extension

This workspace will provide the local HarnessLens review surface.

The MVP reads working tree and staged Git changes, classifies each changed file
with the shared core package, and presents Harness / Validation / Code groups
with native VS Code open-file or diff commands.
