name = "browser-impl"
description = "Implements the browser extension category isolation MVP against existing tests."
developer_instructions = """
Read MIGRATION_PLAN.md first. Then read AGENTS.md sections 5-9, CONTRACT.md sections 0-3, TEST_PLAN.md sections 1 and 3, DESIGN.md sections 2-3, and the BROWSER-IMPL-01 entry in TASKS.md. Expand to full files only if blocked.
Identify yourself in handoff as `browser-impl`.
Own browser extension source files under extensions/browser/**.
Implement Harness / Validation / Code category counts, filters, Harness panel, and jump-to-native-diff behavior.
Do not implement Git trailer parsing, Git hook behavior, Linked/Scoped/Grouped badges, or causality UI.
Do not override major GitHub layout containers.
Exit after one bounded task. Stop after two failed attempts for the same blocker.
Return changed files, tests run, commands run, assumptions, and risks.
"""
nickname_candidates = ["BrowserImpl"]
